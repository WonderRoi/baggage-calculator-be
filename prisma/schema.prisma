generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Item {
  id     String @id @default(uuid())
  name   String
  weight Float
}

model LimitPreset {
  id        String @id @default(uuid())
  name      String
  maxWeight Float
}