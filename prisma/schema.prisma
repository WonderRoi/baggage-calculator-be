generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Item {
  id     String @id @default(uuid())
  name   String
  weight Float
  imageUrl String?
}

model LimitPreset {
  id        String @id @default(uuid())
  name      String
  maxWeight Float
}