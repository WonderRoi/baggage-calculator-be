import { limitPresetTypeDefs } from "./typeDefs";
import { limitPresetResolvers } from "./resolvers";

export const limitPresetModule = {
  typeDefs: limitPresetTypeDefs,
  resolvers: limitPresetResolvers,
};

export { limitPresetTypeDefs, limitPresetResolvers };
